<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/RelativeLayout_actionbar"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/transprant_black"
    android:gravity="center_vertical"
    android:padding="5dp"
    android:visibility="visible" >

    <TextView
        android:id="@+id/btnLogin"
        android:layout_width="300dp"
        android:layout_height="match_parent"
        android:background="@color/dark_blue"
        android:gravity="center_horizontal|center_vertical"
        android:text="Sale Person"
        android:textColor="@color/white"
        android:textSize="20dp" />

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_alignParentRight="true"
        android:gravity="right" >

        <Button
            android:id="@+id/btnChange_mode"
            android:layout_width="250dp"
            android:layout_height="match_parent"
            android:background="@drawable/bg_btn_darkblue"
            android:padding="5dp"
            android:text="Change Picker Mode"
            android:textColor="@color/white"
            android:textSize="20dp"
            android:visibility="visible" />

        <Button
            android:id="@+id/btnCategories"
            android:layout_width="200dp"
            android:layout_height="match_parent"
            android:layout_marginLeft="20dp"
            android:background="@drawable/bg_btn_darkblue"
            android:padding="5dp"
            android:text="Categories"
            android:textColor="@color/white"
            android:textSize="20dp"
            android:visibility="gone" />

    </LinearLayout>

</RelativeLayout>